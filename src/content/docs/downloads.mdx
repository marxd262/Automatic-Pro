---
title: Downloads
description: Compatibility page that points directly to the v2 and vIT3 version pages.
---

import VersionCards from '../../components/VersionCards.astro';
import { releaseFamilies } from '../../lib/releaseData';

Use the version pages below for the actual downloads. They are the primary entry point now:

<VersionCards families={releaseFamilies} />
